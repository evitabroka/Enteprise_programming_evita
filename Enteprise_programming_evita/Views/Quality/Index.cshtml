@model IEnumerable<Enteprise_programming_evita.Models.Quality>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QualityName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QualityName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.QualityId }) |
            @Html.ActionLink("Details", "Details", new { id=item.QualityId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.QualityId })
        </td>
    </tr>
}

</table>
